package xxl.core;

public abstract class IntervalFunction extends Function {
    /* Stores the interval used by the function */
    protected Interval _interval;

    // FIXME maybe toString implementation ?? otherwise its empty
}
